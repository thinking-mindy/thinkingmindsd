/** Auto-generated desktop stub for payments.ts */


export async function getPaymentsForOrg(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: getPaymentsForOrg is not available until this module is migrated to Rust.' };
}

export async function getPaymentsForCurrentOrg(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: getPaymentsForCurrentOrg is not available until this module is migrated to Rust.' };
}

export async function initiatePlanPayment(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: initiatePlanPayment is not available until this module is migrated to Rust.' };
}

export async function checkPlanPaymentStatus(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: checkPlanPaymentStatus is not available until this module is migrated to Rust.' };
}

export async function initiatePOSPayNowPayment(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: initiatePOSPayNowPayment is not available until this module is migrated to Rust.' };
}
