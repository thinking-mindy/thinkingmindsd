/** Auto-generated desktop stub for orgs.ts */


export async function createOrg(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: createOrg is not available until this module is migrated to Rust.' };
}

export async function getOrg(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: getOrg is not available until this module is migrated to Rust.' };
}

export async function syncLocalOrgToDatabase(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: syncLocalOrgToDatabase is not available until this module is migrated to Rust.' };
}

export async function getAllOrgs(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: getAllOrgs is not available until this module is migrated to Rust.' };
}

export async function searchOrgs(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: searchOrgs is not available until this module is migrated to Rust.' };
}

export async function updateOrg(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: updateOrg is not available until this module is migrated to Rust.' };
}

export async function updateOrgPlan(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: updateOrgPlan is not available until this module is migrated to Rust.' };
}

export async function ensureOrgsHavePlans(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: ensureOrgsHavePlans is not available until this module is migrated to Rust.' };
}

export async function deleteOrg(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: deleteOrg is not available until this module is migrated to Rust.' };
}
