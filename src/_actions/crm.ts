/** Auto-generated desktop stub for crm.ts */


export async function createContact(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: createContact is not available until this module is migrated to Rust.' };
}

export async function getContact(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: getContact is not available until this module is migrated to Rust.' };
}

export async function getContactsByOrg(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: getContactsByOrg is not available until this module is migrated to Rust.' };
}

export async function getContactsForCurrentOrg(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: getContactsForCurrentOrg is not available until this module is migrated to Rust.' };
}

export async function searchContacts(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: searchContacts is not available until this module is migrated to Rust.' };
}

export async function updateContact(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: updateContact is not available until this module is migrated to Rust.' };
}

export async function addDealToContact(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: addDealToContact is not available until this module is migrated to Rust.' };
}

export async function updateDealOnContact(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: updateDealOnContact is not available until this module is migrated to Rust.' };
}

export async function deleteDealFromContact(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: deleteDealFromContact is not available until this module is migrated to Rust.' };
}

export async function deleteContact(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: deleteContact is not available until this module is migrated to Rust.' };
}
