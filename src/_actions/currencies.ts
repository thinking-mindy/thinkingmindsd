/** Auto-generated desktop stub for currencies.ts */


export async function createCurrency(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: createCurrency is not available until this module is migrated to Rust.' };
}

export async function getCurrencyByCode(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: getCurrencyByCode is not available until this module is migrated to Rust.' };
}

export async function getCurrency(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: getCurrency is not available until this module is migrated to Rust.' };
}

export async function getAllCurrencies(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: getAllCurrencies is not available until this module is migrated to Rust.' };
}

export async function getUsdExchangeRates(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: getUsdExchangeRates is not available until this module is migrated to Rust.' };
}

export async function updateCurrency(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: updateCurrency is not available until this module is migrated to Rust.' };
}

export async function updateExchangeRates(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: updateExchangeRates is not available until this module is migrated to Rust.' };
}

export async function convertCurrency(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: convertCurrency is not available until this module is migrated to Rust.' };
}

export async function deleteCurrency(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: deleteCurrency is not available until this module is migrated to Rust.' };
}
