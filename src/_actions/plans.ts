/** Auto-generated desktop stub for plans.ts */


export async function ensureDefaultPlans(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: ensureDefaultPlans is not available until this module is migrated to Rust.' };
}

export async function seedDefaultPlans(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: seedDefaultPlans is not available until this module is migrated to Rust.' };
}

export async function createPlan(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: createPlan is not available until this module is migrated to Rust.' };
}

export async function getPlan(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: getPlan is not available until this module is migrated to Rust.' };
}

export async function getPlanBySlug(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: getPlanBySlug is not available until this module is migrated to Rust.' };
}

export async function getAllPlans(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: getAllPlans is not available until this module is migrated to Rust.' };
}

export async function updatePlan(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: updatePlan is not available until this module is migrated to Rust.' };
}

export async function deletePlan(..._args: unknown[]) {
  return { success: false as const, error: 'Desktop app: deletePlan is not available until this module is migrated to Rust.' };
}
